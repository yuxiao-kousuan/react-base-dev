# 左侧导航系统使用指南

## 功能特性

✅ **展开/收起功能** - 点击侧边栏顶部的切换按钮可以展开或收起导航菜单
✅ **响应式设计** - 在移动设备上自动适配
✅ **活跃状态指示** - 当前页面会在导航中高亮显示
✅ **图标支持** - 每个导航项都可以配置图标
✅ **路由集成** - 与React Router完美集成

## 导航结构

当前配置的导航菜单包括：

- 🏠 **欢迎页** (`/`) - 首页
- 📊 **仪表盘** (`/dashboard`) - 数据统计页面
- 👥 **用户管理** (`/users`) - 用户管理页面
- ⚙️ **系统设置** (`/settings`) - 系统配置页面
- 📈 **图表分析** (`/charts`) - 数据可视化页面

## 如何添加新的导航项

### 1. 在路由配置中添加新页面

编辑 `src/router/index.tsx` 文件：

```typescript
const routes: RouteConfig[] = [
    // ... 现有路由
    {
        path: '/new-page',
        component: lazy(() => import('@src/page/NewPage')),
        title: '新页面',
        icon: '🆕',
        showInMenu: true
    }
];
```

### 2. 创建对应的页面组件

在 `src/page/` 目录下创建新的页面文件夹和组件：

```
src/page/NewPage/
├── index.tsx
└── index.less
```

### 3. 配置项说明

- `path`: 路由路径
- `component`: 页面组件（使用懒加载）
- `title`: 导航显示名称
- `icon`: 导航图标（支持emoji）
- `showInMenu`: 是否在导航菜单中显示
- `children`: 子路由（可选，用于多级导航）

## 样式自定义

### 侧边栏样式

主要样式文件：`src/components/Layout/index.less`

- `.sidebar`: 侧边栏容器
- `.sidebar-header`: 侧边栏头部（包含logo和切换按钮）
- `.sidebar-nav`: 导航菜单区域
- `.nav-link`: 导航链接样式
- `.nav-link.active`: 当前活跃页面样式

### 响应式断点

- 桌面端：默认展开状态
- 平板端（≤768px）：侧边栏可收起
- 手机端（≤480px）：侧边栏覆盖模式

## 使用技巧

1. **收起状态**：在收起状态下，只显示图标，鼠标悬停会显示完整标题
2. **移动端**：在移动设备上，侧边栏会以覆盖模式显示，点击遮罩层可关闭
3. **活跃指示**：当前页面的导航项会有特殊的背景色和右侧指示条
4. **图标选择**：建议使用emoji图标，简单直观且兼容性好

## 开发服务器

启动开发服务器：

```bash
npm run dev
```

访问 http://localhost:5173 查看效果。

## 注意事项

- 确保所有页面组件都正确导出为默认导出
- 路由路径不要重复
- 图标建议使用emoji，避免字体图标兼容性问题
- 页面组件应该包含适当的样式文件
